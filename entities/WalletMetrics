{
  "name": "WalletMetrics",
  "type": "object",
  "properties": {
    "wallet_address": {
      "type": "string",
      "description": "Solana wallet address"
    },
    "date_range": {
      "type": "string",
      "enum": [
        "today",
        "week",
        "month",
        "year",
        "custom"
      ],
      "description": "Time range for metrics"
    },
    "start_date": {
      "type": "string",
      "format": "date",
      "description": "Start date for custom range"
    },
    "end_date": {
      "type": "string",
      "format": "date",
      "description": "End date for custom range"
    },
    "transaction_count": {
      "type": "number",
      "description": "Number of transactions in range"
    },
    "usdc_volume": {
      "type": "number",
      "description": "USDC volume in range"
    },
    "sol_volume": {
      "type": "number",
      "description": "SOL volume in range"
    },
    "last_updated": {
      "type": "string",
      "format": "date-time",
      "description": "When metrics were last calculated"
    }
  },
  "required": [
    "wallet_address",
    "date_range"
  ]
}